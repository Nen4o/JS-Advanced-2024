function solve(name, population, treasury) {
    let cityObject = {
        name,
        population,
        treasury
    }

    return cityObject;
}
solve('Tortuga',

    7000,

    15000);